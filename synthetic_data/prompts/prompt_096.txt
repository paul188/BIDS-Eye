You are generating high-quality Text-to-SQL training data for a BIDS neuroimaging database.

Your job is to generate EXACTLY 5 records. Some records may share the same SQL intent,
but only in controlled paraphrase bundles. Diversity matters more than raw volume.

═══════════════════════════════════════════════════════════
SYSTEM PROMPT (this is what the model sees at inference time — match it exactly)
═══════════════════════════════════════════════════════════
You are a Text-to-SQL assistant for BIDS-Eye, a search engine over neuroimaging datasets.

Database schema:

bids_datasets  (d): id UUID PK, name, accession_id, bids_version, dataset_type, source_type, remote_url, validation_status, authors TEXT[], license, doi, paper_references TEXT[], funding TEXT[], description_text TEXT
bids_objects   (o): id UUID PK, dataset_id FK, subject, subject_index, session, task, run, suffix, datatype, extension (file format only: '.nii.gz' etc.), other_entities JSONB (sidecar metadata + secondary entities, e.g. AcquisitionTime, RepetitionTime, acq, echo)
bids_participants(p): id UUID PK, dataset_id FK, participant_id, age FLOAT, sex, handedness, diagnosis (clinical only), extra JSONB (non-standard columns, e.g. concern_dieting, bmi, group)

suffix values : bold=fMRI  T1w/T2w=structural  dwi=diffusion  eeg=EEG  meg=MEG  ieeg=iEEG  pet=PET
datatype values: func  anat  dwi  fmap  eeg  meg  ieeg  beh  pet  perf  nirs

Your SQL MUST always SELECT:
  d.id, d.name, d.accession_id, d.bids_version, d.dataset_type,
  d.source_type, d.remote_url, d.validation_status,
  COUNT(DISTINCT o.subject) AS subject_count

Rules:
- Always alias bids_datasets as d, bids_objects as o, bids_participants as p.
- Always LEFT JOIN bids_objects o ON o.dataset_id = d.id AND o.subject IS NOT NULL.
- Always GROUP BY d.id.
- Use EXISTS (...) to filter by file properties without multiplying rows.
- Use ILIKE '%term%' for case-insensitive text search on d.name and d.description_text.
- Default LIMIT 50 unless the question specifies otherwise.
- Counting: always use COUNT(DISTINCT o.subject) AS subject_count in SELECT. For HAVING thresholds on participant criteria, use COUNT(DISTINCT p.participant_id). Never mix them.
- o.extension is the file format ONLY ('.nii.gz', '.json', '.tsv'). NEVER search extension for metadata field names.
- Sidecar metadata (AcquisitionTime, RepetitionTime, etc.) is in o.other_entities JSONB: o.other_entities->>'AcquisitionTime'
- Non-standard participant fields (bmi, group, custom scales, etc.) are in p.extra JSONB: p.extra->>'concern_dieting'
- p.diagnosis is for clinical diagnoses ONLY. Use p.extra for non-clinical participant attributes.
- The diagnosis, task, suffix, and datatype columns hold normalized standard_codes. Use the exact
  standard_code string in SQL (e.g. p.diagnosis = 'epilepsy', o.task = 'resting_state').
- For BROAD CATEGORIES use a concept key — the system auto-expands it to all specific codes:
    diagnosis concept keys: healthy_cohorts, psychiatric, schizophrenia_spectrum, mood_disorders,
      neurodevelopmental, neurological, epilepsy_spectrum, epilepsy_syndromes, cortical_malformations,
      neurodegenerative, cerebrovascular, movement_disorders, demyelinating_disorders, brain_tumors
    task concept keys: resting_state, working_memory, memory, attention, language, social_cognition,
      reward_learning, motor, sensory, emotion
    suffix concept keys: mri_functional, mri_anatomical, mri_diffusion, mri_fieldmap, electrophysiology
  Example: p.diagnosis = 'epilepsy_spectrum' is expanded to IN ('epilepsy', 'watanabe_syndrome', ...)
  Example: o.task = 'resting_state' is expanded to IN ('resting_state', 'resting_state_eyes_open', ...)
- Output ONLY the SQL query, no explanation.

═══════════════════════════════════════════════════════════
CANONICAL OUTPUT FORMAT (every generated SQL must follow this SELECT structure)
═══════════════════════════════════════════════════════════
Q: Show me all fMRI datasets
```sql
SELECT d.id, d.name, d.accession_id, d.bids_version, d.dataset_type,
       d.source_type, d.remote_url, d.validation_status,
       COUNT(DISTINCT o.subject) AS subject_count
FROM bids_datasets d
LEFT JOIN bids_objects o ON o.dataset_id = d.id AND o.subject IS NOT NULL
WHERE EXISTS (
    SELECT 1 FROM bids_objects bo WHERE bo.dataset_id = d.id AND bo.suffix = 'bold'
)
GROUP BY d.id
ORDER BY d.name
LIMIT 50
```

Q: EEG datasets with more than 30 subjects
```sql
SELECT d.id, d.name, d.accession_id, d.bids_version, d.dataset_type,
       d.source_type, d.remote_url, d.validation_status,
       COUNT(DISTINCT o.subject) AS subject_count
FROM bids_datasets d
LEFT JOIN bids_objects o ON o.dataset_id = d.id AND o.subject IS NOT NULL
WHERE EXISTS (
    SELECT 1 FROM bids_objects bo WHERE bo.dataset_id = d.id AND bo.datatype = 'eeg'
)
GROUP BY d.id
HAVING COUNT(DISTINCT o.subject) > 30
ORDER BY subject_count DESC
LIMIT 50
```

Q: Datasets with Alzheimer's patients
```sql
SELECT d.id, d.name, d.accession_id, d.bids_version, d.dataset_type,
       d.source_type, d.remote_url, d.validation_status,
       COUNT(DISTINCT o.subject) AS subject_count
FROM bids_datasets d
LEFT JOIN bids_objects o ON o.dataset_id = d.id AND o.subject IS NOT NULL
WHERE EXISTS (
    SELECT 1 FROM bids_participants p
    WHERE p.dataset_id = d.id AND p.diagnosis ILIKE '%alzheimer%'
)
GROUP BY d.id
ORDER BY d.name
LIMIT 50
```

Q: Find datasets containing participants with any form of epilepsy
```sql
SELECT d.id, d.name, d.accession_id, d.bids_version, d.dataset_type,
       d.source_type, d.remote_url, d.validation_status,
       COUNT(DISTINCT o.subject) AS subject_count
FROM bids_datasets d
LEFT JOIN bids_objects o ON o.dataset_id = d.id AND o.subject IS NOT NULL
WHERE EXISTS (
    SELECT 1 FROM bids_participants p
    WHERE p.dataset_id = d.id
      AND p.diagnosis = 'epilepsy_spectrum'
)
GROUP BY d.id
ORDER BY d.name
LIMIT 50
```

Q: Datasets that include resting-state scans
```sql
SELECT d.id, d.name, d.accession_id, d.bids_version, d.dataset_type,
       d.source_type, d.remote_url, d.validation_status,
       COUNT(DISTINCT o.subject) AS subject_count
FROM bids_datasets d
LEFT JOIN bids_objects o ON o.dataset_id = d.id AND o.subject IS NOT NULL
WHERE EXISTS (
    SELECT 1 FROM bids_objects bo
    WHERE bo.dataset_id = d.id AND bo.task = 'resting_state'
)
GROUP BY d.id
ORDER BY d.name
LIMIT 50
```

Q: Datasets that have no DWI data
```sql
SELECT d.id, d.name, d.accession_id, d.bids_version, d.dataset_type,
       d.source_type, d.remote_url, d.validation_status,
       COUNT(DISTINCT o.subject) AS subject_count
FROM bids_datasets d
LEFT JOIN bids_objects o ON o.dataset_id = d.id AND o.subject IS NOT NULL
WHERE NOT EXISTS (
    SELECT 1 FROM bids_objects bo WHERE bo.dataset_id = d.id AND bo.datatype = 'dwi'
)
GROUP BY d.id
ORDER BY d.name
LIMIT 50
```

Q: Resting-state fMRI datasets from OpenNeuro with longitudinal data
```sql
SELECT d.id, d.name, d.accession_id, d.bids_version, d.dataset_type,
       d.source_type, d.remote_url, d.validation_status,
       COUNT(DISTINCT o.subject) AS subject_count
FROM bids_datasets d
LEFT JOIN bids_objects o ON o.dataset_id = d.id AND o.subject IS NOT NULL
WHERE d.source_type = 'openneuro'
  AND EXISTS (
    SELECT 1 FROM bids_objects bo
    WHERE bo.dataset_id = d.id AND bo.suffix = 'bold' AND bo.task ILIKE '%rest%'
  )
  AND (SELECT COUNT(DISTINCT bo2.session) FROM bids_objects bo2
       WHERE bo2.dataset_id = d.id AND bo2.session IS NOT NULL) >= 2
GROUP BY d.id
ORDER BY subject_count DESC
LIMIT 50
```

═══════════════════════════════════════════════════════════
REAL DATABASE SIGNALS
═══════════════════════════════════════════════════════════
Common tasks: ['resting_state', 'resting_state_eyes_closed', 'resting_state_eyes_open', 'movie_viewing', 'attention_and_executive_control', 'n_back_working_memory', 'video_game_and_video', 'Stimulation', 'working_memory_general', 'social', 'main', 'ltpFR', 'szMonitoring', 'seizure', 'contrastChangeDetection', 'Passive', 'ltpFR2', 'rat', 'surroundSupp', 'bao']
Long-tail tasks: ['None', 'RingRest', 'FFERN', 'MultisensoryDetectionTask', 'phantom', 'rew', 'DMNTRACKINGTRAIN', 'ERNPsychometrics', 'MASK', 'MORALDILEMMA', 'PEER1', 'PEER2', 'polar', 'reactive', 'DiscountFix', 'DMNTRACKINGTEST', 'bold', 'biofieldtherapy', 'auditorystimuli', 'UnevenTerrain']
Common suffixes: ['EEG', 'MEG']
Long-tail suffixes: []
Datatypes: ['functional_mri', 'electroencephalography', 'anatomical_mri', 'intracranial_eeg', 'field_maps', 'diffusion_mri', 'magnetoencephalography', 'behavioural_data', 'perfusion_asl', 'fnirs', 'positron_emission_tomography', 'motion_capture', 'magnetic_resonance_spectroscopy']
Diagnoses: ['healthy control (standard)', 'patient', 'healthy_control_task_variant', 'healthy_volunteer', 'anxiety_disorder', 'autism_spectrum_disorder', 'major_depressive_disorder', 'typically_developing', 'young', 'parkinsons_disease', 'older', 'synucleinopathy', 'schizophrenia', 'healthy_control_processing_variant', 'focal_cortical_dysplasia', '2mo_control', 'Dyslexia', 'depression', 'misinfo', 'asd sibling', 'bipolar_disorder', 'fibromyalgia', 'prosopagnosia', 'younger', 'mbsr+', 'old', 'smh', 'ADHD', '9mo_control', 'use_both']
Sex values: ['female', 'male']
Handedness values: ['ambidextrous / mixed-handed']
Common object JSON keys: ['onset', 'duration', 'value', 'sample', 'event_code', 'acquisition', 'hed_defs', 'hed_def', 'hed_definitions', 'type', 'ProtocolName', 'ImageComments', 'BandwidthPerPixelPhaseEncode', 'Manufacturer', 'TxRefAmp', 'WipMemBlock', 'SequenceVariant', 'ImagingFrequency', 'BaseResolution', 'ImageOrientationPatientDICOM']
Common participant extra keys: ['sedation', 'field', 'coil', 'breathing', 'dataset', 'med.bolus', 'med.route', 'med.infusion', 'isoflurane', 'Age', 'Sex', 'EF', 'rLEF', 'weight', 'efs_runs', 'date', 'rs_runs', 'duration', 'protocol', 'Race']
Authors (top contributors): ['Michael Milham', 'Arnaud Delorme', 'David R. Roalf', 'Taylor Salo', 'Matt Cieslak', 'Joanes Grandjean', 'Theodore D. Satterthwaite', 'Daniel H. Wolf', 'M. Dylan Tisdall', 'Ruben C. Gur', 'Brooke L. Sevchik', 'Monica E. Calkins', 'Kosha Ruparel', 'Sage Rush', 'Kahini Mehta', 'Tyler M. Moore', 'Raquel E. Gur', 'James F Cavanagh', 'James R. Booth', 'Eishi Asano']
Licenses in use: ['CC0', 'PDDL', 'This dataset is made available under the Public Domain Dedication and License \nv1.0, whose full text can be found at \nhttp://www.opendatacommons.org/licenses/pddl/1.0/. \nWe hope that all users will follow the ODC Attribution/Share-Alike \nCommunity Norms (http://www.opendatacommons.org/norms/odc-by-sa/); \nin particular, while not legally required, we hope that all users \nof the data will acknowledge the OpenfMRI project and NSF Grant \nOCI-1131441 (R. Poldrack, PI) in any publications.', 'CC-BY-SA 4.0', 'Creative Commons Attribution 4.0 International (CC-BY 4.0)', 'CCO', 'This data is made available under the Creative Commons BY-SA 4.0 International License.', 'PD', 'Creative Commons CC0 1.0', 'Creative Commons Attribution 4.0 International Public License', 'This dataset is made available under the Public Domain Dedication and License \nv1.0, whose full text can be found at \nhttp://www.opendatacommons.org/licenses/pddl/1.0/. \nWe hope that all users will follow the ODC Attribution/Share-Alike \nCommunity Norms (http://www.opendatacommons.org/norms/odc-by-sa/); \nin particular, while not legally required, we hope that all users \nof the data will acknowledge the OpenfMRI project and Ian Ballard in any publications.', 'CC BY', 'PPDL', 'https://creativecommons.org/licenses/by-nc-sa/4.0/', 'Creative Commons Attribution-NonCommercial 4.0 International']
Funding sources: ['TODO', 'GRANT #1', 'GRANT #2', 'N/A', 'See https://childmind.org/science/global-open-science/healthy-brain-network/#donors', 'Canada First Research Excellence Fund "BrainsCAN" grant to Western University', 'NIH/NIMH R01MH125934 for BIDS data preparation', 'Western University internal funds for Jody Culham', 'DARPA RAM: N66001-14-2-4032', 'This research was sponsored by the Army Research Laboratory and was accomplished under Cooperative Agreement Number W911NF-10-0-0002.', 'ERC-StG-2018-803370', 'CI241/3-1', 'ARC DP200101787 (TAC)', 'CI241/1-1']
Datasets with a DOI: 1598 (use d.doi IS NOT NULL or ILIKE to match)
Description search terms (real words from description_text): ['participants', 'cognitive', 'modafinil', 'within', 'single', 'behaviors', 'found', 'brain', 'activity', 'folders', 'control', 'fmri', 'performed', 'four', 'healthy', 'corresponds', 'visit', 'adults', 'respective', 'related', 'folder', 'attention', 'fluid', 'anterior', 'task', 'insula', 'network', 'participant', 'valence', 'between']
  → use d.description_text ILIKE '%term%' or d.name ILIKE '%term%' for topic searches
Participant age (p.age FLOAT, NULL if not recorded):
  range 0–100 yrs | mean 27.5 | p25=18 p75=32
  → age filters: WHERE p.age BETWEEN lo AND hi  |  WHERE p.age > threshold
  → age aggregates: SELECT AVG(p.age) ... GROUP BY d.id  |  HAVING AVG(p.age) > threshold

Broad concept keys (auto-expanded by post-processor — use in SQL instead of listing all sub-codes):
  - diagnosis.neurological  (47 sub-codes)
  - diagnosis.epilepsy_spectrum  (13 sub-codes)
  - diagnosis.tumors_lesions  (11 sub-codes)
  - diagnosis.tumor_spectrum  (9 sub-codes)
  - diagnosis.psychiatric  (8 sub-codes)
  - diagnosis.neurodevelopmental  (8 sub-codes)
  - diagnosis.healthy_cohorts  (6 sub-codes)
  - diagnosis.cortical_malformations  (6 sub-codes)
  - diagnosis.spinal_neuromuscular  (6 sub-codes)
  - diagnosis.cerebrovascular  (5 sub-codes)
  - diagnosis.movement_disorders  (5 sub-codes)
  - diagnosis.sensory_metabolic_trauma  (5 sub-codes)
  - diagnosis.mood_disorders  (4 sub-codes)
  - diagnosis.stroke_spectrum  (4 sub-codes)
  - diagnosis.glioma_spectrum  (4 sub-codes)
  - diagnosis.schizophrenia_spectrum  (3 sub-codes)
  - diagnosis.depression_spectrum  (3 sub-codes)
  - diagnosis.learning_disorders  (3 sub-codes)
  - diagnosis.epilepsy_syndromes  (3 sub-codes)
  - diagnosis.focal_cortical_dysplasia  (3 sub-codes)
  - diagnosis.benign_tumors  (3 sub-codes)
  - diagnosis.parkinsons_spectrum  (3 sub-codes)
  - diagnosis.physical_health  (3 sub-codes)
  - diagnosis.genetic_syndromes  (2 sub-codes)
  - diagnosis.other_lesions  (2 sub-codes)
  - diagnosis.degenerative_dementia  (2 sub-codes)
  - diagnosis.cervical_myelopathy_spectrum  (2 sub-codes)
  - diagnosis.als_spectrum  (2 sub-codes)
  - diagnosis.traumatic_brain_injury_spectrum  (2 sub-codes)
  - diagnosis.visual_impairment_spectrum  (2 sub-codes)
  - task.resting_state  (5 sub-codes)
  - task.naturalistic  (3 sub-codes)
  - task.working_memory  (2 sub-codes)
  - suffix.mri_anatomical  (12 sub-codes)
  - suffix.mri_quantitative_and_misc  (11 sub-codes)
  - suffix.electrophysiology  (8 sub-codes)
  - suffix.mri_functional  (6 sub-codes)
  - suffix.field_maps  (6 sub-codes)
  - suffix.pet_asl  (6 sub-codes)
  - suffix.other_modalities  (5 sub-codes)
  - suffix.mrs_spectroscopy  (2 sub-codes)
  - datatype.other  (6 sub-codes)
  - datatype.mri  (5 sub-codes)
  - datatype.electrophysiology  (3 sub-codes)

Numeric sidecar keys (cast required — these values are TEXT in JSONB):
  - o.other_entities->>'BandwidthPerPixelPhaseEncode' → e.g. 41.667  (use ::float for numeric comparisons)
  - o.other_entities->>'TxRefAmp' → e.g. 259.028  (use ::float for numeric comparisons)
  - o.other_entities->>'ImagingFrequency' → e.g. 123.262321  (use ::float for numeric comparisons)
  - o.other_entities->>'BaseResolution' → e.g. 64.0  (use ::float for numeric comparisons)

Sidecar metadata fields (o.other_entities JSONB) — use the natural-language synonym in the question, the code in SQL:
  - synonym: subject position, scanner orientation, patient orientation  →  o.other_entities->>'PatientPosition'
  - synonym: scan time, time of acquisition, scanning time  →  o.other_entities->>'AcquisitionTime'
  - synonym: TR, repetition interval, pulse repetition time  →  o.other_entities->>'RepetitionTime'
  - synonym: TE, echo delay, echo time value  →  o.other_entities->>'EchoTime'
  - synonym: excitation angle, RF flip angle, pulse flip angle  →  o.other_entities->>'FlipAngle'
  - synonym: field strength, scanner field strength, MRI field strength  →  o.other_entities->>'MagneticFieldStrength'
  - synonym: scanner brand, MRI manufacturer, scanner maker  →  o.other_entities->>'Manufacturer'  (e.g. 'Siemens')
  - synonym: scanner model name, MRI model, device model  →  o.other_entities->>'ManufacturersModelName'
  - synonym: slice acquisition times, slice onset times  →  o.other_entities->>'SliceTiming'
  - synonym: task description, paradigm name  →  o.other_entities->>'TaskName'
  - synonym: scanning site, acquisition site, institution  →  o.other_entities->>'InstitutionName'

Participant extra fields (p.extra JSONB) — use the natural-language synonym in the question, the code in SQL:
  - synonym: rapid injection, intravenous bolus, drug bolus  →  p.extra->>'med.bolus'  (e.g. '0.05')
  - synonym: route of administration, drug delivery method, administration path  →  p.extra->>'med.route'  (e.g. 'i.v.')
  - synonym: dieting concern, dietary concern, concern about diet  →  p.extra->>'concern_dieting'
  - synonym: BMI score, body mass index value, weight-to-height ratio  →  p.extra->>'bmi'
  - synonym: study group, participant group, cohort group  →  p.extra->>'group'
  - synonym: education level, years of schooling, schooling years  →  p.extra->>'education_years'
  - synonym: ethnic background, racial identity, ethnic identity  →  p.extra->>'ethnicity'
  - synonym: smoker status, tobacco use, cigarette use  →  p.extra->>'smoking'
  - synonym: drinking habits, alcohol consumption, alcohol history  →  p.extra->>'alcohol_use'
  - synonym: intelligence score, cognitive score, full scale iq  →  p.extra->>'iq'

Sample datasets:
  - ds000052 | 'Classification learning and reversal' | n_participants=0 | datatypes=['anatomical_mri', 'functional_mri'] | tasks=['reversalweatherprediction', 'weatherprediction']
  - ds003602 | 'Childhood Sexual Abuse and problem drinking in women: Neurobehavioral mechanisms' | n_participants=118 | datatypes=['electroencephalography'] | tasks=['APA1', 'APA2', 'FP3', 'resting_state', 'VP1', 'VP3']
  - ds004520 | 'Changes in behavioral priority influence the accessibility of working memory content - Experiment 2\n' | n_participants=0 | datatypes=['electroencephalography'] | tasks=['Retrocue']
  - ds004852 | 'InsurgentCivilian' | n_participants=1 | datatypes=['electroencephalography'] | tasks=['n_back_working_memory']
  - ds004956 | 'Braille letters - fMRI' | n_participants=15 | datatypes=['anatomical_mri', 'functional_mri'] | tasks=['letters', 'localizer']
  - ds005258 | 'Keep Control Validation Dataset' | n_participants=2 | datatypes=['motion_capture'] | tasks=['calibration1', 'calibration2', 'calibration3', 'calibration4', 'tug', 'walkFast', 'walkPreferred', 'walkSlow']
  - ds005594 | 'Alphabetic Decision Task (Arial Light Font)' | n_participants=16 | datatypes=['electroencephalography'] | tasks=['alphabeticdecision']
  - ds006905 | 'Organizational Skills Training' | n_participants=84 | datatypes=['anatomical_mri', 'field_maps', 'functional_mri'] | tasks=['fix', 'inscapes', 'movie_viewing']

Longitudinal examples:
  - ds006465 | 4 sessions
  - ds007602 | 15 sessions
  - ds005572 | 2 sessions
  - ds002862 | 2 sessions
  - ds004974 | 3 sessions

Multimodal examples:
  - ds007508 | datatypes=['anatomical_mri', 'field_maps', 'functional_mri']
  - ds000133 | datatypes=['anatomical_mri', 'functional_mri']
  - ds002513 | datatypes=['anatomical_mri', 'field_maps', 'functional_mri']
  - ds005533 | datatypes=['anatomical_mri', 'functional_mri']
  - ds006654 | datatypes=['anatomical_mri', 'functional_mri']

═══════════════════════════════════════════════════════════
GENERATION RULES
═══════════════════════════════════════════════════════════
1. Every SQL query must be valid PostgreSQL for this schema.
2. Use only fields and values that are plausible from the schema and signals above.
   - For sex: ONLY use values from the "Sex values" list above. Never invent values like drug names, numbers, or anything not in that list.
   - For handedness: ONLY use values from the "Handedness values" list above.
   - For diagnosis: ONLY use values from the "Diagnoses" list above or well-known clinical terms (e.g. "Parkinson's disease", "depression"). Never use drug names, procedures, or non-clinical terms. p.diagnosis is for clinical group labels ONLY.
   - For tasks, suffixes, datatypes: ONLY use values from the provided lists.
   - CRITICAL: o.extension contains ONLY the file format suffix (e.g. '.nii.gz', '.json', '.tsv').
     It NEVER contains metadata keys like 'AcquisitionTime', 'RepetitionTime', 'bold', 'T1w', etc.
     Any query of the form WHERE o.extension = 'AcquisitionTime' or WHERE o.extension::text LIKE '%bold%'
     is ALWAYS wrong. For acquisition parameters use o.other_entities->>'KeyName' instead.
   - Sidecar metadata fields (AcquisitionTime, RepetitionTime, EchoTime, etc.) live in o.other_entities JSONB. Access them with: o.other_entities->>'AcquisitionTime'.
   - Non-standard participant attributes (BMI, questionnaire scores, custom columns, etc.) live in p.extra JSONB. Access them with: p.extra->>'concern_dieting'. Do NOT put these in p.diagnosis.
   - For broad categories (epilepsy, resting-state, psychiatric, etc.) use the concept key listed above.
     The system post-processes concept keys into full IN (...) lists — you MUST write only the concept key.
     Example: p.diagnosis = 'epilepsy_spectrum'  (not  p.diagnosis IN ('epilepsy', 'watanabe_syndrome', ...))
     Example: o.task = 'resting_state'            (not  o.task IN ('resting_state', 'resting_state_eyes_open', ...))
3. Do not overuse one SQL skeleton.
4. Use a real mix of SQL structures:
   - JOIN + DISTINCT
   - EXISTS
   - NOT EXISTS
   - GROUP BY + HAVING
   - ranking with ORDER BY / LIMIT
   - aggregate queries
5. Every SELECT must include ALL of the following columns — no exceptions, no omissions:
     d.id, d.name, d.accession_id, d.bids_version, d.dataset_type,
     d.source_type, d.remote_url, d.validation_status,
     COUNT(DISTINCT o.subject) AS subject_count
   Do not add any extra output columns beyond these unless the question explicitly asks for them.
6. For "all / only / exclusively" participant constraints, prefer alias-aware logic.
7. A paraphrase bundle should contain only 2 to 3 questions for the same SQL intent.
8. Paraphrases must be meaning-preserving, not tiny wording edits.
9. At least 40% of the batch should use SQL structures other than plain JOIN + WHERE.
10. At least 25% of the batch should use long-tail values from the database signals.
11. Never put field names, column names, or technical keys in quotes or apostrophes inside the question text.
12. Counting convention: COUNT(DISTINCT o.subject) AS subject_count is always in the SELECT (rule 5 above).
    For HAVING thresholds that filter on participant criteria (sex, diagnosis, etc.), use COUNT(DISTINCT p.participant_id) — never use subject_count in HAVING.
    Never mix the two. Write natural sentences: "datasets where acquisition time is recorded" not "datasets with 'AcquisitionTime'". Use the synonyms from the sidecar/extra field lists above.
13. Age queries (family = age_query): p.age is a FLOAT column in bids_participants (NULL when not recorded).
    - Range filter:    WHERE p.age BETWEEN lo AND hi
    - Threshold:       WHERE p.age > threshold  /  WHERE p.age < threshold
    - Aggregate:       SELECT AVG(p.age) AS mean_age  (add as an extra column for these questions only)
    - HAVING mean age: GROUP BY d.id HAVING AVG(p.age) > threshold
    Use natural phrasing in questions (e.g. "studies with elderly participants", "datasets covering ages 18 to 30")
    rather than exposing raw column names.
14. Subject-level AND/OR queries (family = subject_multimodal_query):
    "Same subject has both fMRI and EEG" requires a double EXISTS correlated on (dataset_id, subject).
    A dataset-level HAVING check only proves a dataset HAS both types — not that one person has both.
    Always use the double EXISTS pattern shown in the intent note.
    Conditions can mix any bids_objects columns: datatype, task, suffix, or any combination.
15. Numeric JSON comparisons (family = json_numeric_query):
    JSONB values in o.other_entities are stored as TEXT. For numeric comparisons you MUST cast:
      (o.other_entities->>'RepetitionTime')::float > 2.0
      (o.other_entities->>'NumberOfVolumes')::int >= 200
    Never write: o.other_entities->>'RepetitionTime' > '2.0'  (string comparison — wrong result).
    See "Numeric sidecar keys" above for keys confirmed to have numeric values in this database.

═══════════════════════════════════════════════════════════
PARAPHRASE POLICY
═══════════════════════════════════════════════════════════
- This prompt includes 2 planned paraphrase bundles.
- For a paraphrase bundle, keep one SQL intent and produce 2-3 stylistically distinct questions.
- Good paraphrase styles: concise, formal/scientific, natural/casual, indirect.
- Never change thresholds, negation, requested output, or comparison meaning.

═══════════════════════════════════════════════════════════
INTENT PLAN FOR THIS PROMPT
═══════════════════════════════════════════════════════════
Use this plan as coverage guidance. You may refine wording, but you must preserve the
family, SQL-structure intent, and paraphrase counts.

```json
[
  {
    "family": "subject_multimodal_query",
    "sql_structure": "exists",
    "paraphrase_count": 2,
    "focus_values": {
      "condition_a_col": "datatype",
      "condition_a_val": "perfusion_asl",
      "condition_b_col": "task",
      "condition_b_val": "video_game_and_video",
      "connective": "OR"
    },
    "notes": [
      "Subject has EITHER datatype='perfusion_asl' OR task='video_game_and_video' (or both).\nSQL \u2014 two EXISTS joined with OR:\n  WHERE (\n      EXISTS (SELECT 1 FROM bids_objects o1\n             WHERE o1.dataset_id = d.id AND o1.datatype = 'perfusion_asl'\n               AND o1.subject = <subject_from_outer>)\n      OR\n      EXISTS (SELECT 1 FROM bids_objects o2\n             WHERE o2.dataset_id = d.id AND o2.task = 'video_game_and_video'\n               AND o2.subject = <subject_from_outer>)\n  )\nAlternatively: check at the dataset level with two separate EXISTS if the question is about dataset coverage rather than individual subject."
    ]
  },
  {
    "family": "multimodal_query",
    "sql_structure": "ranking_limit",
    "paraphrase_count": 1,
    "focus_values": {
      "modalities": [
        "anatomical_mri",
        "magnetoencephalography"
      ]
    },
    "notes": [
      "use explicit modality presence checks"
    ]
  },
  {
    "family": "author_query",
    "sql_structure": "filtered_aggregate",
    "paraphrase_count": 1,
    "focus_values": {
      "author": "Kosha Ruparel"
    },
    "notes": [
      "use '= ANY(d.authors)' or ILIKE ANY(d.authors) for author lookup"
    ]
  },
  {
    "family": "metadata_query",
    "sql_structure": "join_distinct",
    "paraphrase_count": 2,
    "focus_values": {
      "other_entities_key": "SequenceVariant",
      "extra_key": "sedation",
      "sidecar_field_code": "PatientPosition",
      "sidecar_field_synonym": "prone vs supine",
      "extra_field_code": "smoking",
      "extra_field_synonym": "tobacco use"
    },
    "notes": [
      "sidecar metadata lives in o.other_entities JSONB (use ->>), non-standard participant fields in p.extra JSONB (use ->>). NEVER search o.extension for metadata field names. Use the natural-language synonym (sidecar_field_synonym / extra_field_synonym) in the question \u2014 no apostrophes or technical key names in the question text.",
      "use o.other_entities->>'FieldName' for sidecar metadata fields. o.extension is file format only ('.nii.gz') \u2014 never use it for metadata lookups."
    ]
  },
  {
    "family": "doi_query",
    "sql_structure": "join_distinct",
    "paraphrase_count": 1,
    "focus_values": {},
    "notes": [
      "filter WHERE d.doi IS NOT NULL or match a specific DOI pattern with ILIKE"
    ]
  }
]
```

═══════════════════════════════════════════════════════════
SELF-REVIEW (mandatory before returning output)
═══════════════════════════════════════════════════════════
Before emitting the final JSON array, silently review every generated pair
against this checklist.  Fix any pair that fails — do not include it broken.

For each (question, sql) pair verify:
  [ ] SELECT contains all 9 mandatory columns: d.id, d.name, d.accession_id,
      d.bids_version, d.dataset_type, d.source_type, d.remote_url,
      d.validation_status, COUNT(DISTINCT o.subject) AS subject_count
  [ ] No extra SELECT columns unless the question explicitly requires them
  [ ] o.extension is NOT used for metadata lookups (file format only)
  [ ] JSONB numeric comparisons use ::float or ::int cast, not string compare
  [ ] sex / handedness values are from the provided lists only
  [ ] diagnosis uses standard clinical terms or concept keys — not drug names
  [ ] Concept keys are used as-is (e.g. p.diagnosis = 'epilepsy_spectrum'),
      NOT expanded into IN (...) lists
  [ ] Subject-level AND queries use double EXISTS correlated on subject,
      not a dataset-level HAVING check
  [ ] HAVING clauses for participant criteria use COUNT(DISTINCT p.participant_id),
      never subject_count
  [ ] The question uses natural language — no raw column names, key names,
      or internal codes in apostrophes

═══════════════════════════════════════════════════════════
OUTPUT FORMAT
═══════════════════════════════════════════════════════════
Return ONLY a JSON array.
Each element must have:
  - "question"
  - "sql"
  - "pattern"
  - "family"
  - "sql_structure"
  - "paraphrase_bundle_id"  (null if standalone)

Use short pattern labels if you want, but keep them consistent.
Prompt index: 96
